<div class="right_col" role="main">
  <div class="page-title">
    <div class="title_left">
      <h1>Usuarios</h1>
    </div>
  </div>
  <div class="clearfix"></div>
  
  <div class="col-md-12 col-sm-12 col-xs-12">
    <div class="row">
      <div class="col-ms-12">
        <div class="div-add-button">

           <%= link_to new_usuario_path, {remote: true, class: "btn btn-info btn-lg btn-add", id: "anadir-categoria-btn"}  do %>
             <span class="glyphicon glyphicon-plus-sign circleglyphicon" style=""></span>
             Añadir usuario
          <%end%>

        </div>
      </div>
    </div>
    
    <hr style="border-style: solid; border-width: 2px;">

    <div class="row">
      <div>
      
        <table class="table table-striped responsive-utilities jambo_table responsive no-wrap data-table" id="tabla_categorias">
          <thead>
            <tr>
            <th class="column-title">Nombreusuario</th>
            <th class="column-title">Contrasena</th>
            <th class="column-title">Persona</th>
              <th class="column-title">Tipousuario</th>
              <th class="column-title"></th>
              <th class="column-title"></th>
            </tr>
          </thead>

          <tbody id="tb_usuarios">
            <%= render @usuarios%>
            <!--<% @usuarios.each do |usuario| %>
      <tr>
        <td><%= usuario.nombreUsuario %></td>
        <td><%= usuario.contrasena %></td>
        <td><%= usuario.persona_id %></td>
        <td><%= usuario.tipoUsuario_id %></td>
        <td><%= link_to 'Show', usuario %></td>
        <td><%= link_to 'Edit', edit_usuario_path(usuario) %></td>
        <td><%= link_to 'Destroy', usuario, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>-->
          </tbody>
        </table>

        <%= render 'modalUsuarios'%>
      </div>
    </div>
  </div>