// Add the dialog title
$('#modal-impuesto-title').html("<i class='glyphicon glyphicon-plus'></i> Crear nuevo impuesto");

// Render the new form
$('#modal-body-impuesto').html('<%= j render("form") %>');

// Show the dynamic dialog
$('#modalImpuestos').modal("show");

// Set focus to the first element
$('#modalImpuestos').on('shown.bs.modal', function () {
	  $('.first_input').focus();
})
