<%= form_for(@marca_producto) do |f| %>
  <% if @marca_producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marca_producto.errors.count, "error") %> prohibited this marca_producto from being saved:</h2>

      <ul>
      <% @marca_producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
