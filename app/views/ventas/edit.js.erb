// Add the dialog title
$('#modal-ventas-title').html("<i class='fa fa-trash'></i> Cancelar Venta");
 
// Render the new form
$('.modal-body-ventas').html('<%= j render("cancelaVenta") %>');
 
// Show the dynamic dialog
$('#modalVentas').modal("show");
 
// Set focus to the first element
$('#modalVentas').on('shown.bs.modal', function () {
	  $('.first_input').focus();
})