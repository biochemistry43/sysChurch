// Add the dialog title
$('#modal-claves-title').html("<i class='glyphicon glyphicon-plus'></i> Crear clave de Producto o servicio");

// Render the new form
$('.modal-body-clave-productos').html('<%= j render("form") %>');

// Show the dynamic dialog
$('#modalClaveProductos').modal("show");

// Set focus to the first element
$('#modalClaveProductos').on('shown.bs.modal', function () {
	  $('.first_input').focus();
})
