<tr id="Proveedor_<%= proveedor.id%>">
  <td><%= proveedor.nombre %></td>
  <td><%= proveedor.telefono %></td>
  <td><%= proveedor.email %></td>
  <td><%= link_to 'Editar', edit_proveedor_path(proveedor), html_options = {class: "btn btn-info btn-xs", remote: true} %><i class="fa fa-pencil"></i></td>
  <td><%= link_to 'Borrar', proveedor, remote: true, method: :delete, data: { confirm: "¿Borrar categoria: #{proveedor.nombre}?"}, class: "btn btn-danger btn-xs" %><i class="fa fa-trash-o"></td>
</tr>